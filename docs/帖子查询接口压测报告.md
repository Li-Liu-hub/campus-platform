# 帖子查询接口压测报告

## 一、测试目标

（待填写：例如验证万级数据量下帖子条件查询和按 ID 查询的响应性能）

## 二、测试环境

| 项目 | 内容 |
| --- | --- |
| 应用地址 | http://localhost:8080（Docker 容器 campushub-app） |
| 数据库 | MySQL 8.4，campushub 库 |
| 帖子数据量 | （待填写：SELECT COUNT(*) FROM ch_post） |
| 压测工具 | JMeter |
| 耗时采集方式 | JMeter 聚合报告、应用内 StopWatch 日志、Tomcat 访问日志（%D，微秒） |

## 三、JMeter 配置

### 线程组

| 配置项 | 值 |
| --- | --- |
| 线程数 | （待填写） |
| Ramp-Up 时间（秒） | （待填写） |
| 循环次数 | （待填写） |

### HTTP 请求

```text
请求方法：GET
请求地址：http://localhost:8080/api/v1/posts/query
查询参数：（待填写：userName / postType / postTitle / sortOrder）
```

### 请求头

| 参数名 | 参数值 |
| --- | --- |
| Authorization | Bearer （登录后获取的 Token） |

## 四、测试场景

（待填写：例如全量查询、按分类查询、按标题模糊查询、组合条件查询、按 ID 查询）

## 五、压测结果

### 聚合报告

| 场景 | 样本数 | 平均值(ms) | 中位数(ms) | 90%百分位(ms) | 95%百分位(ms) | 99%百分位(ms) | 最小(ms) | 最大(ms) | 异常% | 吞吐量(/s) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| （待填写） |  |  |  |  |  |  |  |  |  |  |

### 应用层耗时（StopWatch 日志）

| 场景 | SQL 耗时(ms) | 命中条数 |
| --- | --- | --- |
| （待填写） |  |  |

### Tomcat 访问日志耗时

| 场景 | %D 数值(微秒) | 换算毫秒 |
| --- | --- | --- |
| （待填写） |  |  |

## 六、结果分析

（待填写：对比各场景耗时差异，分析 SQL 执行、数据传输、序列化等环节的开销占比）

## 七、结论

（待填写）
